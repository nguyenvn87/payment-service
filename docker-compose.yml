version: '3.7'

services:
  appsvc:
    image: nguyenvnu87/payment-service:1.0
    networks:
      - my_network
    deploy:
      replicas: 3
    ports:
      - '8899:8899'
networks:
  my_network:
    driver: overlay